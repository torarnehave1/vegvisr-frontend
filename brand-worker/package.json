{
  "name": "brand-worker",
  "version": "1.0.0",
  "description": "Brand domain proxy worker for Vegvisr",
  "main": "index.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "wrangler dev --local"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
